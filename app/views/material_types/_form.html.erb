<%= form_with(model: material_type, local: true) do |form| %>
  <% if material_type.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(material_type.errors.count, "error") %> prohibited this material_type from being saved:</h2>

      <ul>
        <% material_type.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
